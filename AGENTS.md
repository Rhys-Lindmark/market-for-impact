# GiveBetter x SF research provenance

Before researching a specific organization, read and follow [docs/research-effort.md](docs/research-effort.md). Record actual dedicated research start/end intervals and the verified runtime AI model; never substitute the analytical cost-effectiveness model version. Stop the interval before switching organizations, integration/deployment, or idle waiting. Capture author, modeling and independent audit sessions separately, with stable researcher/session IDs. Use the timestamping helper and preserve its output outside the Site checkout until root integration.

Only the root Site owner may edit this checkout, integrate provenance, run Sites tools, commit, push or deploy. Research workers return isolated artifacts without spawning nested agents.

Every new report and substantive research revision must add available organization-specific provenance to data/research-effort.json. Historical missing intervals stay explicitly unknown/partial. Never divide a project/batch duration among reports or infer a model from current settings. Run provenance validation and responsive report-header checks before release. Use the Space Saver skill for sustained work and reuse compatible dependencies.
