import {calculate} from '@/lib/bvhpf-model.mjs';
import {calculateResidenceSensitivity} from '@/lib/bvhpf-residence-model.mjs';
function labelServiceGeography(row:Record<string,unknown>){const {bayQaly,sfQaly,bayImpactShare,sfImpactShare,...rest}=row;return {...rest,serviceSiteAttributed:{bayQaly,sfQaly,bayImpactShare,sfImpactShare}};}
export function GET(){const base=calculate();const {bayShareOfCreditedQaly,sfShareOfCreditedQaly,...inputs}=base.inputs;return Response.json({modelVersion:'bvhpf-whole-org-v1',geographyVersion:'resident-allocation-v1',verifiedMarginalFundingOffer:null,interpretation:'Total health is unchanged. SF65%/Bay90% resident allocation is a subjective prior, not measured residence. Verified SF service location is a separate quantity.',evaluated:{...base,inputs:{...inputs,serviceSiteAttributed:{bayShareOfCreditedQaly,sfShareOfCreditedQaly}},scenarios:base.scenarios.map(labelServiceGeography),weighted:labelServiceGeography(base.weighted)},geography:calculateResidenceSensitivity(base)});}
