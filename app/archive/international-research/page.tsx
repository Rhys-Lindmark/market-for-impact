// Keep old bookmarks working under either Sites or the canonical /givebetter proxy.
// Re-export the new archive without a root-relative redirect escaping the base path.
export {default,metadata} from '../expanded-geography-research/page';
