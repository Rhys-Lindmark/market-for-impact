# BayLegal portable model acceptance

**ACCEPT corrected model for exploratory unfavorable publication; HOLD giving.** Portable standalone ESM: `/private/tmp/mfi-bay-area-legal-aid-model.mjs`; regression tests: `/private/tmp/mfi-bay-area-legal-aid-esm.test.mjs`. Original CommonJS and every coefficient preserved. ESM has no imports, network, filesystem, dependencies or execution side effects.

Entire default output deep-equals corrected CommonJS baseline. Expected QALY0.04092667307156319; donor/Bay$24,433,943.07305236 per10. Historical pro-rata audited-resource proxy remains distinct from null verified marginal/complete gross fields. Full$30,238,844 cost and$3,896,303 housing partner subgrants retained; no separate partner recipients added. Finite utility-duration packages remain unchanged, at most3years, signed harm retained.

Necessary validation-only corrections: rejectNaN/Infinity in inputs/scenarios and computed outputs; nonnegative zero gift now returns zero/null; positive denominators and modeled program expenses/counts required; malformed/missing/extra program ledgers rejected; summed ledgers cannot exceed full expense; partner subgrants cannot exceed housing ledger; scenarios require finite weights, unique names and a favorableStress weight<1 for defined removal diagnostic; SF share must nest in Bay. Unsupported scenario arrays now fail clearly instead of divide-by-zero/undefined failures. Null inputs still select defaults; null scenarios explicitly fail.

Tests pass: exact whole-object baseline parity, zero gift, all-additionality zero, signed/null preservation,2×scaling, NaN/Infinity, zero denominators, malformed programs/scenarios, nested geography and extreme-input overflow. Baseline coefficients, rounding and scenario weights untouched.

Bay100% semantics: three populations are described as Bay service populations. This is not an audited residence file. Keep final report's service-population wording and note unknown current SF residence;15–25%SF are priors. Geography price equality is model construction, not independent proof of100%current Bay beneficiaries. Whole cost is retained while other service benefits are unquantified, not known zero.

Coordinated with policy worker: final renderer file `/private/tmp/mfi-baylegal-report-final.json`; donation URL omitted as required; modelVersion `bay-area-legal-aid-whole-org-v1`. Root may choose endpoint naming. No new public source claims researched in this conversion.

Actual UTC interval2026-09-11 04:46:32–04:57:07 (10m35s wall elapsed including tool delays; not claimed continuously active). gpt-6-astra/low per parent dispatch metadata. Space Saver: existing Node, small pure artifacts only; no Site edits/tools, dependencies, previews, outreach or nested agents.

Integration note: added explicit filingEntityEin and clarified narrative identity versus unverified payment route. No numerical inputs or results changed.
