# ReCARES final integration audit

Verdict: **ACCEPT the inspected integration for exploratory publication; no material model, source-content or ranking bug found. Giving remains HOLD.** Root's browser/deployment checks remain the independent runtime gate; this review does not claim those checks passed.

Actual UTC start2026-09-11 01:19:37; inspection checkpoint01:20:40; final base-path comparison and artifact completed01:21:33 (1m56s). Root-confirmed dispatch gpt-6-astra / low, UI label GPT-6 Astra Lite. Read-only inspection of `work/market-for-impact-recares`; no Site edits or outreach.

## Checked and passed

1. **Default numerical parity:** imported production `lib/recares-model.mjs` and retained accepted v2 model in memory. Deep equality of the entire result object after removing only `publicationStatus`. Inputs, all five worlds, pathways, weighted outputs and diagnostics are unchanged. Bay QALYs0.537893882075896; Bay cost/10 **185910.27585974694**; total180970.9925659543; SF486998.267198264. Favorable-tail share69.2830676%. New immutable10k cap and finite cost validation do not change default outputs.
2. **Report evidence:** `data/bay/recares-report.json` contains a rendered controlled-aids row and matching primary trial sources. It states functional improvements do not establish randomized QoL gains, the rollator null finding and8/18 infrequent use, the0–100 rating is not utility, and installed-shower evidence is not portable-aid evidence. No new trial coefficient was silently substituted.
3. **Inspectable API:** `app/api/recares-model/route.ts` returns original inputs, all five scenario objects with three full mix rows each, source ledger and the full evaluated result. It exposes signed null/harm and complete-resource/marginal-offer null fields, not merely a central scalar. GET accepts no custom inputs. The report page adds a real native model link.
4. **Bay ranking:** `lib/bay-research-index.ts` computes `.weighted` and binds ReCARES to `.bayDonorCostPer10Qaly`, with the separate SF field. It does not use central, total, favorable or gross benefit. Homepage picks derive their number from the unified Bay-primary row.
5. **Homepage caution:** ReCARES says “not a verified giving recommendation or marginal funding offer,” discloses69% favorable-tail dependence, unknown unique recipients/safe use/cash capacity, and unvalued equipment/volunteer resources. Its illustration panel says exploratory and unverified. General page headings remain “Top Charities,” but the ReCARES-specific language does not claim a verified offer or assured return.
6. **Effort metadata:** executed registry validation and `researchEffortSummary`. Result is exactly **Research time: ~13 min on GPT-6 Astra Lite**, from759seconds /12.65minutes. Four intervals:389s authoring,94s first audit,165s utility check,111s integration checklist. Coverage is partial; earlier unknown work is not backfilled. Same-worker intervals do not overlap, and concurrent different-worker work is legitimate summed effort.
7. **Provenance qualifications:** model comment now states chronology is author-reported and not independently verified. The two health-worker audit registry endpoints extend past their documents' evidence-inspection endpoints; entries explicitly cite completion messages as well. Those messages are not reproduced here, so I verify arithmetic/registry consistency, not independently certify those extra finalization seconds. My own authoring and utility intervals match recorded phase timings.
8. **Dates:** publication string10September2026 is consistent with the user's America/Los_Angeles date during these September11UTC evening checks. Sources retrievedSeptember11UTC are not a future-date error. Model status is now `exploratory_research`, not stale draft status. Root must only count publication after actual canonical deployment.

## Canonical model-link check

The source link is `/api/recares-model`, consistent with existing report wrappers. I checked the currently published Berkeley Free Clinic analogue: canonical HTML rewrites its native link to `/givebetter/api/berkeley-free-clinic-model`; the serialized component retains the unprefixed source path. The unprefixed canonical-origin API returns404, so root's browser gate should verify the **rendered** ReCARES link is similarly rewritten, rather than treating the source string alone as proof. This is an existing reverse-proxy behavior, not an identified ReCARES code bug. Its new route still requires post-deployment smoke testing.

## Nonblocking scope limits

No browser suite was rerun, no deployment performed, and no source efficacy coefficients revalidated beyond comparison with the already accepted primary-evidence check. The18/26/etc. test counts belong to their actual executing agent; this document claims only the independent in-memory parity and metadata checks described above. Uncertainty remains substantial, but uncertainty alone is not a new integration defect.
