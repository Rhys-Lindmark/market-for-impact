import{test,expect}from'@playwright/test';test('city theory exposes complete neutral model ledger and research order',async({page,request})=>{await page.goto('/research/city-theory');await expect(page.getByRole('heading',{name:'What makes city giving cost-effective?',exact:true})).toBeVisible();await page.locator('.city-cohort summary').click();await expect(page.locator('[data-model-ein]')).toHaveCount(25);await expect(page.locator('body')).toContainText('No interim top-four reselection');const r=await request.get('/api/sf-city-theory');expect(r.ok()).toBeTruthy();const j=await r.json();expect(j.registry.rows).toHaveLength(25);expect(j.theory.requiredQalysPerDollar).toBe(.0001);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
