import{test,expect}from'@playwright/test';test('MNC dental research separates incidence from untreated pain',async({page,request})=>{await page.goto('/charities/mission-neighborhood-centers');await expect(page.getByRole('heading',{name:'Mission Neighborhood Centers',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$39.3M');await expect(page.locator('body')).toContainText('not an existing verified MNC varnish program');const r=await request.get('/api/sf-mnc-model');expect(r.ok()).toBeTruthy();const m=await r.json();expect(m.evaluatedScenarios[1].costPerTenQalys).toBeCloseTo(39256978.3629,2);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
