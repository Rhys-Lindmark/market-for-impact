import{test,expect}from'@playwright/test';test('New Door separates health-only judgment from economic mobility',async({page,request})=>{await page.goto('/charities/new-door-ventures');await expect(page.getByRole('heading',{name:'New Door Ventures',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$160M');await expect(page.locator('body')).toContainText('not a measured New Door health effect');const r=await request.get('/api/sf-newdoor-model');expect(r.ok()).toBeTruthy();expect((await r.json()).evaluatedScenarios[1].costPerTenQalys).toBe(160000000);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
