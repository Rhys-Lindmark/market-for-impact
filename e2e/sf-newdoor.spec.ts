import{test,expect}from'@playwright/test';test('New Door separates whole-gift partial health from economic mobility',async({page,request})=>{await page.goto('/charities/new-door-ventures');await expect(page.getByRole('heading',{name:'New Door Ventures',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$508 million');await expect(page.locator('body')).toContainText('entire unrestricted gift');const r=await request.get('/api/newdoor-portfolio-model');expect(r.ok()).toBeTruthy();const d=await r.json();expect(d.evaluated).toHaveLength(8);expect(d.evaluated[0].sfUsdPer10Qaly).toBeCloseTo(507936507.9365,2);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
