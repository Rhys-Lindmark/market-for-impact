import{test,expect}from'@playwright/test';test('Openhouse separates companionship assumptions from measured health',async({page,request})=>{await page.goto('/charities/openhouse');await expect(page.getByRole('heading',{name:'Openhouse',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$5.33M');await expect(page.locator('body')).toContainText('not Openhouse housing');const r=await request.get('/api/sf-openhouse-model');expect(r.ok()).toBeTruthy();expect((await r.json()).evaluatedScenarios[1].costPerTenQalys).toBeCloseTo(5333333.333333334);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
