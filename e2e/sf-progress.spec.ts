import{test,expect}from'@playwright/test';test('Progress crisis comparison retains credible null and payer boundary',async({page,request})=>{await page.goto('/charities/progress-foundation');await expect(page.getByRole('heading',{name:'Progress Foundation',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$391M');await expect(page.locator('body')).toContainText('at least as credible');const r=await request.get('/api/sf-progress-model');expect(r.ok()).toBeTruthy();expect((await r.json()).evaluatedScenarios[1].costPerTenQalys).toBeCloseTo(391071428.5714286);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
