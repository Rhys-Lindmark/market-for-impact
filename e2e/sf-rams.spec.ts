import {test,expect} from '@playwright/test';test('RAMS exposes the modality boundary and modeled QALYs',async({page,request})=>{await page.goto('/charities/rams');await expect(page.getByRole('heading',{name:'Richmond Area Multi-Services (RAMS)',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$4.21M');await expect(page.locator('body')).toContainText('not a verified RAMS CBT program');const r=await request.get('/api/sf-rams-model');expect(r.ok()).toBeTruthy();expect((await r.json()).evaluatedScenarios[1].costPerTenQalys).toBeCloseTo(4210526.315789474);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
