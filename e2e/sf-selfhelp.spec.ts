import{test,expect}from'@playwright/test';test('Self-Help exposes proposed curriculum and unit ambiguity',async({page,request})=>{await page.goto('/charities/self-help-for-the-elderly');await expect(page.getByRole('heading',{name:'Self-Help for the Elderly',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$2.4M');await expect(page.locator('body')).toContainText('$600K');const r=await request.get('/api/sf-selfhelp-model');expect(r.ok()).toBeTruthy();expect((await r.json()).evaluatedScenarios[1].costPerTenQalys).toBe(2400000);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
