import{test,expect}from'@playwright/test';test('THC shows short-horizon turnover model',async({page,request})=>{await page.goto('/charities/tenderloin-housing-clinic');await expect(page.getByRole('heading',{name:'Tenderloin Housing Clinic',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$46.8M');const r=await request.get('/api/sf-thc-model');expect(r.ok()).toBeTruthy();expect((await r.json()).evaluatedScenarios[1].costPerTenQalys).toBeCloseTo(46794871.79487179);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
