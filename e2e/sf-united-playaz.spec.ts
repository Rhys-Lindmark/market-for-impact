import{test,expect}from'@playwright/test';test('United Playaz exposes conditional engagement and mortality threshold',async({page,request})=>{await page.goto('/charities/united-playaz');await expect(page.getByRole('heading',{name:'United Playaz',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('$26.7M');await expect(page.locator('body')).toContainText('conditional on effective engagement');const r=await request.get('/api/sf-united-playaz-model');expect(r.ok()).toBeTruthy();expect((await r.json()).evaluatedScenarios[1].costPerTenQalys).toBeCloseTo(26666666.666666668);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBeTruthy();});
