function valid(s,ks){for(const k of ks)if(!Number.isFinite(s[k])||s[k]<0)throw new RangeError('Invalid '+k);if(s.transfer>1||s.fundingAdditionality>1)throw new RangeError('Invalid fraction');}
function result(cost,health,s){const netQalys=s.fundingAdditionality===0?0:s.fundingAdditionality*(health*s.transfer-s.harmQalys);return{donorCost:cost,netQalys,costPerTenQalys:netQalys>0?10*cost/netQalys:null,maximumCostFor100k:Math.max(0,netQalys)*10000};}
export function respiteModel(s){valid(s,['days','dailyCost','externalQalys','transfer','fundingAdditionality','harmQalys']);return result(s.days*s.dailyCost,s.externalQalys,s);}
export function woundModel(s){valid(s,['cost','ulcerFreeWeeks','utilityGap','transfer','fundingAdditionality','harmQalys']);if(s.ulcerFreeWeeks>52||s.utilityGap>1)throw new RangeError('Invalid health state');return result(s.cost,s.ulcerFreeWeeks/52*s.utilityGap,s);}
